import React from "react";
import { Spin } from "antd";

const LoadingComponent = () => {
  return <Spin size="large" style={{ width: "100%", padding: "80px 0" }} />;
};

export default LoadingComponent;
